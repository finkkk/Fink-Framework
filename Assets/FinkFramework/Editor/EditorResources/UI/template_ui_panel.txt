// ===============================================
// 自动生成文件（请按需求手动修改）
// 生成时间：#DATE#
// 面板名称：#CLASSNAME#
// ===============================================

#USINGS#

namespace #NAMESPACE#
{
    /// <summary>
    /// UI 面板：#CLASSNAME#
    /// 自动生成的基础逻辑，请在本类中扩展业务代码。
    /// </summary>
    public class #CLASSNAME# : BasePanel
    {
#FIELDS#
        
        protected override void Awake()
        {
            base.Awake();

            // 自动生成：控件绑定
            // BasePanel 会自动扫描控件并缓存。
            // 这里只在需要长期持有引用时，通过 GetControl<T>(name) 获取。
#ASSIGN#
        }

        protected override void OnInitialize(UIPanelContext context)
        {
            // 实例创建后只执行一次：建立长期监听、缓存数据源等。
        }

        protected override void OnEnter()
        {
            // 每次打开时执行：刷新显示数据、重置临时状态等。
            // 若启动异步流程，请传入 Context.VisibilityToken；面板关闭时会自动取消。
        }

        protected override void OnPause()
        {
            // 被导航栈中的上层页面覆盖时执行。
        }

        protected override void OnResume()
        {
            // 上层页面退出、当前页面恢复交互时执行。
        }

        protected override void OnExit()
        {
            // 每次关闭时执行：停止临时动画、保存临时状态等。
            // 此时 Context.VisibilityToken 已被取消，无需重复处理显示周期内的异步任务。
        }

        protected override void OnDispose()
        {
            // 实例销毁前只执行一次：取消长期监听并释放引用。
        }

        // ============================================================
        // 以下事件由 BasePanel 统一分发
        // 若该面板存在对应类型的控件则自动触发
        // ============================================================

        /// <summary>
        /// 按钮点击事件回调
        /// btnName = 按钮对象名
        /// </summary>
        protected override void OnButtonClicked(string btnName)
        {
            #LOGIC_BUTTON#
        }

        /// <summary>
        /// 滑动条数值改变回调
        /// sliderName = 滑条对象名
        /// value = 当前值
        /// </summary>
        protected override void OnSliderValueChanged(string sliderName, float value)
        {
            #LOGIC_SLIDER#
        }

        /// <summary>
        /// InputField 内容改变回调
        /// inputName = 输入框对象名
        /// value = 内容
        /// </summary>
        protected override void OnInputValueChanged(string inputName, string value)
        {
            #LOGIC_INPUT#
        }
        
        /// <summary>
        /// Toggle 勾选状态变化回调
        /// toggleName = 控件对象名
        /// value = 是否勾选
        /// </summary>
        protected override void OnToggleValueChanged(string toggleName, bool value)
        {
            #LOGIC_TOGGLE#
        }
    }
}
